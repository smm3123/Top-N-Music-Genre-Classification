const Algorithm: React.FC = () => {
  return (
    <div className="algorithm">
      <h1>How Our Algorithm Works</h1>
      <p>
        Explainer page - goes into methods and details on neural network
        (training, set up, etc.)
      </p>
    </div>
  );
};

export default Algorithm;
